import { useSession } from "../hooks/useSession";
import { ResponsiveContainer,LineChart,Line,XAxis,YAxis,Tooltip,BarChart,Bar } from "recharts";
export default function Statistics(){const {user}=useSession();if(!user)return null;const s=user.stats;const line=Array.from({length:12},(_,i)=>({round:i+1,profit:Math.round(s.totalProfit*(i+1)/12)}));const bars=[["Wins",s.wins],["Losses",s.losses],["Rounds",s.rounds]];return <div><div className="page-title"><p className="eyebrow">PERFORMANCE LAB</p><h1>Statistics</h1><p>Track your virtual arcade performance over time.</p></div><StatList user={user}/><div className="chart-grid"><div className="panel chart"><h2>Profit curve</h2><ResponsiveContainer width="100%" height={280}><LineChart data={line}><XAxis dataKey="round"/><YAxis/><Tooltip/><Line type="monotone" dataKey="profit" strokeWidth={3}/></LineChart></ResponsiveContainer></div><div className="panel chart"><h2>Round mix</h2><ResponsiveContainer width="100%" height={280}><BarChart data={bars}><XAxis dataKey="0"/><YAxis/><Tooltip/><Bar dataKey="1"/></BarChart></ResponsiveContainer></div></div></div>}
function StatList({user}:any){const s=user.stats;return <div className="stat-grid">{[["Rounds",s.rounds],["Wins",s.wins],["Losses",s.losses],["Win Rate",`${s.rounds?Math.round(s.wins/s.rounds*100):0}%`],["Wagered",s.totalWagered],["Biggest Win",s.biggestWin],["Biggest Loss",s.biggestLoss],["Favorite",s.favoriteGame]].map(([a,b])=><div className="stat-card" key={a}><span>{a}</span><strong>{typeof b==="number"?b.toLocaleString():b}</strong></div>)}</div>}
